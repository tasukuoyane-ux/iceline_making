# 管理画面（/console・/console/check・/admin）セットアップ・運用ガイド

サイトのコンテンツを、エンジニア以外の社員がブラウザから編集し、本番サイトへ反映できる管理画面群です。

| 画面 | URL | 役割 |
| --- | --- | --- |
| 管理コンソール | `https://<本番ドメイン>/console` | ページの文言・画像・SEO の編集（タブ: ページ編集 / SEO） |
| セルフチェック | `https://<本番ドメイン>/console/check` | 公開前チェックリストの自動検査＋手動確認リスト |
| 記事管理 | `https://<本番ドメイン>/admin` | お知らせ記事の追加・編集・削除（Payload CMS） |

- 文言・画像の編集 → コンソール右上の「更新（本番へ公開）」→ 数十秒〜数分で本番サイトに反映。
- お知らせ記事の編集 → /admin で保存・公開 → 即時（数秒）で本番サイトに反映。

---

## 仕組み（概要）

```
/console（社員ログイン: CONSOLE_USERS + JWT）
  ├─ ヘッダー: タブ（ページ編集 / SEO）・ページ選択・SP/PC切替・
  │            プレビュー再読み込み・セルフチェック・お知らせ管理・公開ボタン
  ├─ 左：本番ページのライブプレビュー（要素をクリックで右側に編集フォーム）
  └─ 右：タブごとの編集パネル
        ・ページ編集: プレビュー中ページの編集要素一覧（DOM順）
          ＋ ページに応じた管理パネル（/videos=動画、/recruit・/recruit2=採用動画・
             会社紹介資料・インタビュー、/recruit3=背景動画、/contact=送信先設定）
          ＋ 各要素の「SPで非表示」「PCで非表示」トグル
        ・SEO: OGP画像／メタディスクリプション／キーワード（site:seo.* に保存）
        │
        └─「更新」ボタン
              → /api/publish が src/content/*.json を GitHub へコミット
              → Vercel が自動で再ビルド & 本番反映
        画像・動画アップロードは /api/upload 経由で Vercel Blob に保存

/admin（Payload CMS。アカウントは /console とは別に DB 管理）
  └─ お知らせ記事（日付・カテゴリ・タイトル・本文ブロック）→ Neon (Postgres) に保存
        → 公開サイトは /api/news（＋HTML埋め込み）経由で表示。公開すると即時反映

/console/check（/console と同じアカウント）
  ├─ 自動検査: /api/check が環境変数・HTTP出力・DB・GitHubトークンを検査
  └─ 手動確認リスト: Neon の console_check テーブルに保存（DB未接続時は端末ローカル）
```

- **画像・動画の置き場（2026-09 改修）**: /console からアップロードした画像は Vercel Blob ではなく
  リポジトリの `public/uploads/` に GitHub 経由でコミットされる（同名ファイルは上書き・1枚 4MB まで）。
  アップロード時のコミットは `[vercel skip]` 付きでデプロイされず、「更新（本番へ公開）」のデプロイで
  一緒に公開される。動画（採用ページ背景動画・MV動画・動画一覧の mp4 等）は容量が大きいため
  コンソールからはアップロードできず、エンジニアが `public/videos/` に置いて（1本 50MB 以下推奨、
  100MB 超は GitHub が拒否）URL 欄に `/videos/ファイル名.mp4` を入力する。Vercel Blob を使うのは
  /admin の記事添付だけ。Blob 上の旧ファイルの取り込みは `pnpm migrate:blob`（Blob ブロック解除後に実行）。
- ページの文言・画像の実データは `src/content/*.json`
  （`videos.json` / `interviews.json` / `images.json` / `sections.json` / `overrides.json` /
  `profileSlides.json` / `contact.json`）。
- **お知らせ記事は Payload（Neon）へ移行済み**。`news.json` は移行元データ兼
  「DB未接続・記事0件時のフォールバック」として残している（コンソールからは編集不可）。

---

## 初期セットアップ（エンジニアが一度だけ実施）

### 1. 依存パッケージのインストール

```bash
pnpm install
```

### 2. 社員アカウントを作る（/console 用）

パスワードのハッシュを生成します（社員ごとに実行）。

```bash
node scripts/hash-password.mjs <社員ID> <表示名> <パスワード>
# 例: node scripts/hash-password.mjs tanaka 田中太郎 himitsu123
```

出力された JSON を集めて、配列形式で環境変数 `CONSOLE_USERS` に設定します。

```json
[
  {"username":"tanaka","name":"田中太郎","passwordHash":"$2a$10$..."},
  {"username":"suzuki","name":"鈴木花子","passwordHash":"$2a$10$..."}
]
```

### 3. GitHub トークンを用意

本番リポジトリの **Contents 書き込み権限** を持つトークンを発行します
（Fine-grained personal access token 推奨：対象リポジトリの Contents = Read and write）。

### 4. Vercel Blob ストアを作成

Vercel ダッシュボード → Storage → Blob で **public** ストアを作成し、プロジェクトにリンクします。
`BLOB_READ_WRITE_TOKEN` は自動で注入されます。

### 5. Neon (Postgres) を接続（/admin 用）

Vercel ダッシュボード → Storage → Neon を Connect します。
`POSTGRES_URL` / `POSTGRES_URL_NON_POOLING` が自動で注入されます。
あわせて `PAYLOAD_SECRET`（`openssl rand -hex 32` 等で生成）を環境変数に設定します。

### 6. Vercel に環境変数を登録

`.env.example` を参照し、Vercel のプロジェクト設定（Environment Variables）に登録：

| 変数名 | 説明 |
| --- | --- |
| `JWT_SECRET` | /console のログインセッション署名鍵（長いランダム文字列） |
| `CONSOLE_USERS` | 社員アカウントのJSON配列（上記2で作成） |
| `GITHUB_TOKEN` | GitHub の Contents 書き込みトークン |
| `GITHUB_OWNER` | リポジトリのオーナー（例: `tasukuoyane-ux`） |
| `GITHUB_REPO` | リポジトリ名 |
| `GITHUB_BRANCH` | 本番ブランチ（既定 `main`） |
| `BLOB_READ_WRITE_TOKEN` | Vercel Blob（Blobストア作成で自動付与） |
| `POSTGRES_URL` ほか | Neon（Connect で自動付与） |
| `PAYLOAD_SECRET` | Payload のセッション署名・暗号化キー |
| `RESEND_API_KEY` / `CONTACT_FROM` / `CONTACT_RECIPIENT` | お問い合わせフォームのメール送信 |

### 7. DB マイグレーションと記事の移行

デプロイ時に `scripts/vercel-build.mjs` が未適用のマイグレーションだけを自動適用します
（`src/migrations/` が空の場合はローカルで `npx payload migrate:create initial` を実行して
マイグレーションを生成し、コミットしてください。あわせて `scripts/vercel-build.mjs` の
`APPLIED_PROBES` にプローブを1行追加）。

旧CMSのお知らせ（`src/content/news.json`）を Payload へ取り込むには：

```bash
pnpm migrate:news   # 冪等（何度実行しても安全。slug=旧IDで上書き）
```

### 8. /admin の初期管理ユーザーを作成

デプロイ後（またはローカル `pnpm dev`）に `/admin` を開くと、初回のみ
「最初のユーザー作成」画面が表示されます。**必ず早めに会社の管理者アカウントを作成**
してください（0名のままだと最初にアクセスした人が管理者になれる状態です。
この状態は /console/check の自動検査でも NG として警告されます）。

---

## 社員向けの使い方

### ページの文言・画像を直す（/console）

1. `https://<本番ドメイン>/console` を開き、社員IDとパスワードでログイン。
2. ヘッダーのプルダウンから編集したいページを選ぶ（SP/PC ボタンで表示幅を切替）。
3. プレビュー内の **編集したい要素（見出し・本文・画像など）をクリック** すると、
   右側に対応する編集フォームが開きます。
4. テキストを書き換える／画像を「アップロード」または URL で差し替える。
   各要素の「SPで非表示」「PCで非表示」で画面幅ごとに要素を隠すこともできます。
5. 動画・インタビュー・会社紹介資料・お問い合わせ送信先は、該当ページを
   プレビューすると右パネル上部に管理パネル（追加・削除・並べ替え）が出ます。
6. 「SEO」タブで OGP画像・メタディスクリプション・キーワードを設定できます。
7. 一通り編集したら、右上の **「更新（本番へ公開）」** を押す。数十秒〜数分で反映。

> 編集途中の内容はブラウザに自動保存され、ページを閉じても残ります。
> 「変更を破棄」で、最後に公開した状態に戻せます。
> 本番が別の人の公開で更新されていた場合、古い下書きは安全のため自動破棄されます。

### 採用ページ・採用記事の主な設定（2026-09 改修分）

- **職種ピルの色分け**: 「採用」タブ → 各職種の「分類（ピルの色）」で
  食品事業部（赤）／アイス事業部（青）／総務部（グレー）／未分類（黒）を選びます。
- **人を知る**: 見出しとリード文はカルーセル（記事カード）の下に表示されます。
- **カルーセル下の動画キャプション**: 採用ページをプレビューし、動画の下の
  「動画キャプション」をクリックして入力（未入力の間は非表示）。
- **カンパニーデックの一文**: 見出し下の「カンパニーデック リード」に入力（未入力の間は非表示）。
- **動画グリッド（採用ページ最下部）のキャプション**: 動画管理のタイトルが既定。採用ページ側だけ
  文言・色を変えたいときはプレビュー内のキャプションをクリックして編集します。
- **採用記事（/admin の「採用記事」）**: 「自己紹介」「趣味」はアイキャッチ内（氏名の下）に表示。
  「アイキャッチ動画（ファイル）」または「アイキャッチ動画URL」を設定すると、記事のアイキャッチに
  再生ボタンが出て、クリックで画面中央に大きく再生されます。「人を知る」カードには再生ボタンが薄く
  重なり、カードの画像は「メイン画像」（未設定なら動画の1フレーム目）になります。
- **会社情報の拠点情報**: 会社情報ページをプレビューし、「拠点情報」の各項目（名称・住所・電話・
  GoogleマップURL）を編集。地図は住所（「〒」以降）で検索して埋め込まれます。「追加」「削除」で拠点数を変更できます。

### お知らせ記事を書く（/admin）

1. `https://<本番ドメイン>/admin` を開き、記事管理のアカウントでログイン
   （/console のアカウントとは別です）。
2. 「お知らせ」→「新規作成」。日付・カテゴリ・タイトルを入れ、本文はブロック
   （段落／見出し／画像／動画）を並べて作成します。
   段落内では `**太字**`・`==マーカー==` の記法が使えます。
3. 「下書き保存」で非公開のまま保存、「公開」で本番サイトに即時反映されます。
4. 記事のURLは `/news/<ID>`。ID は空のまま保存すると日付から自動生成されます。

### 公開前の確認（/console/check）

/console ヘッダーの「セルフチェック」から開きます（アカウント共通）。
「再検査」で自動検査を実行し、NG項目を潰します。右の手動確認リストは
確認できたら ○ で消し込みます（Neon 接続済みならチームで共有されます）。

---

## トラブルシュート

- **ログインできない（/console）**: `JWT_SECRET` と `CONSOLE_USERS` が登録されているか確認。
- **「更新」で失敗する**: `GITHUB_TOKEN` の権限（Contents=write）と `GITHUB_OWNER/REPO/BRANCH` を確認。
- **画像アップロードに失敗**: Vercel Blob ストアがリンクされ `BLOB_READ_WRITE_TOKEN` があるか確認。
- **反映されない**: GitHub にコミットは入っているか（Vercel のデプロイログを確認）。
- **/admin が開けない・エラーになる**: `POSTGRES_URL`（Neon Connect）と `PAYLOAD_SECRET` を確認。
  マイグレーション未適用の場合はデプロイログの `payload migrate` を確認。
- **お知らせが表示されない/古い**: /admin で記事が「公開」状態か確認。
  記事0件・DB未接続の間は移行元 `news.json` の内容が表示されます。
- 環境の状態は `/api/diag`、まとめての検査は `/console/check` が使えます。
